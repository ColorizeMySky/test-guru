<div class="container">
  <h1><%= t('.title') %></h1>

  <table class="table table-striped">
    <thead>
      <tr>
        <th><%= t('activerecord.attributes.test.name') %></th>
        <th><%= t('activerecord.attributes.test.level') %></th>
        <th><%= t('tests.test.questions_count', count: 0) %></th>
        <th><%= t('actions') %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @tests %>
    </tbody>
  </table>
</div>
